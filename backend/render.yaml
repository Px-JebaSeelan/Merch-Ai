# Render deployment configuration
services:
  - type: web
    name: merch-ai-backend
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        sync: false # Set this manually in Render dashboard
